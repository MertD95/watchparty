// AUTO-GENERATED by watchparty-server/tools/gen-protocol.js
// Source of truth: watchparty-server/src/protocol-v21.ts + events.ts + schemas.ts
// Do not edit manually - run: node tools/gen-protocol.js
//
// Loaded as a content script before all other WatchParty scripts.
// Provides the WPProtocol global used by the extension runtime.

const WPProtocol = (() => {
  'use strict';

  const COMMAND = Object.freeze({
    SESSION_HELLO: 'session.hello',
    ROOM_CREATE: 'room.create',
    ROOM_JOIN: 'room.join',
    ROOM_LEAVE: 'room.leave',
    ROOM_REJOIN: 'room.rejoin',
    ROOM_CHAT_SEND: 'room.chat.send',
    ROOM_REACTION_SEND: 'room.reaction.send',
    ROOM_BOOKMARK_ADD: 'room.bookmark.add',
    ROOM_SETTINGS_UPDATE: 'room.settings.update',
    ROOM_VISIBILITY_UPDATE: 'room.visibility.update',
    ROOM_OWNERSHIP_TRANSFER: 'room.ownership.transfer',
    ROOM_READY_CHECK_UPDATE: 'room.readyCheck.update',
    ROOM_PLAYBACK_PUBLISH: 'room.playback.publish',
    ROOM_CONTENT_UPDATE: 'room.content.update',
    ROOM_MEMBER_PRESENCE_PUBLISH: 'room.member.presence.publish',
    ROOM_MEMBER_PLAYBACK_STATUS_PUBLISH: 'room.member.playbackStatus.publish',
    SESSION_CLOCK_PING: 'session.clock.ping',
    ROOM_TYPING_SEND: 'room.typing.send',
  });

  const EVENT = Object.freeze({
    SESSION_READY: 'session.ready',
    SESSION_USER_UPDATED: 'session.user.updated',
    SESSION_CLOCK_PONG: 'session.clock.pong',
    ROOM_SNAPSHOT: 'room.snapshot',
    ROOM_MEMBER_UPSERTED: 'room.member.upserted',
    ROOM_MEMBER_REMOVED: 'room.member.removed',
    ROOM_MEMBER_PRESENCE_UPDATED: 'room.member.presence.updated',
    ROOM_MEMBER_PLAYBACK_STATUS_UPDATED: 'room.member.playbackStatus.updated',
    ROOM_CHAT_APPENDED: 'room.chat.appended',
    ROOM_CHAT_HISTORY: 'room.chat.history',
    ROOM_REACTION_APPENDED: 'room.reaction.appended',
    ROOM_BOOKMARK_APPENDED: 'room.bookmark.appended',
    ROOM_PLAYBACK_UPDATED: 'room.playback.updated',
    ROOM_PLAYBACK_AUTOPAUSED: 'room.playback.autopaused',
    ROOM_SETTINGS_UPDATED: 'room.settings.updated',
    ROOM_VISIBILITY_UPDATED: 'room.visibility.updated',
    ROOM_OWNERSHIP_UPDATED: 'room.ownership.updated',
    ROOM_CONTENT_UPDATED: 'room.content.updated',
    ROOM_READY_CHECK_UPDATED: 'room.readyCheck.updated',
    ROOM_READY_CHECK_COUNTDOWN: 'room.readyCheck.countdown',
    ROOM_TYPING_UPDATED: 'room.typing.updated',
    ROOM_ERROR: 'room.error',
  });

  const ERROR_CODE = Object.freeze({
    ROOM_NOT_FOUND: 'ROOM_NOT_FOUND',
    ROOM_KEY_REQUIRED: 'ROOM_KEY_REQUIRED',
    INVALID_ROOM_KEY: 'INVALID_ROOM_KEY',
    NOT_OWNER: 'NOT_OWNER',
    COOLDOWN: 'COOLDOWN',
    VALIDATION_FAILED: 'VALIDATION_FAILED',
    PARSE_ERROR: 'PARSE_ERROR',
    USER_NOT_FOUND: 'USER_NOT_FOUND',
    RATE_LIMITED: 'RATE_LIMITED',
  });

  const PROTOCOL_VERSION = 2;

  const DEFAULT_PLAYER = Object.freeze({ paused: true, buffering: true, time: 0 });

  const LIMITS = Object.freeze({
    MESSAGE_MAX: 300,
    USERNAME_MAX: 25,
    ROOM_NAME_MAX: 30,
    BOOKMARK_LABEL_MAX: 100,
  });

  return { COMMAND, EVENT, ERROR_CODE, PROTOCOL_VERSION, DEFAULT_PLAYER, LIMITS };
})();
