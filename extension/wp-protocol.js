// AUTO-GENERATED by watchparty-server/tools/gen-protocol.js
// Source of truth: watchparty-server/src/schemas.ts + events.ts
// Do not edit manually — run: node tools/gen-protocol.js
//
// Loaded as a content script before all other WatchParty scripts.
// Provides the WPProtocol global used by stremio-content.js, stremio-ws.js, etc.

const WPProtocol = (() => {
  'use strict';

  // Client → Server message types
  const C2S = Object.freeze({
    USER_UPDATE: 'user.update',
    ROOM_NEW: 'room.new',
    ROOM_JOIN: 'room.join',
    ROOM_LEAVE: 'room.leave',
    ROOM_MESSAGE: 'room.message',
    ROOM_UPDATE_OWNERSHIP: 'room.updateOwnership',
    ROOM_UPDATE_STREAM: 'room.updateStream',
    PLAYER_SYNC: 'player.sync',
    CLOCK_PING: 'clock.ping',
    ROOM_TYPING: 'room.typing',
    ROOM_REACTION: 'room.reaction',
    ROOM_UPDATE_PUBLIC: 'room.updatePublic',
    USER_PRESENCE: 'user.presence',
    USER_PLAYBACK_STATUS: 'user.playbackStatus',
    ROOM_UPDATE_SETTINGS: 'room.updateSettings',
    ROOM_READY_CHECK: 'room.readyCheck',
    ROOM_BOOKMARK: 'room.bookmark',
    ROOM_REJOIN: 'room.rejoin',
  });

  // Server → Client message types
  const S2C = Object.freeze({
    READY: 'ready',
    USER: 'user',
    ROOM: 'room',
    SYNC: 'sync',
    PLAYER_SYNC: 'playerSync',
    PRESENCE_UPDATE: 'presenceUpdate',
    PLAYBACK_STATUS_UPDATE: 'playbackStatusUpdate',
    SETTINGS_UPDATE: 'settingsUpdate',
    MESSAGE: 'message',
    CHAT_HISTORY: 'chatHistory',
    ERROR: 'error',
    READY_CHECK: 'readyCheck',
    COUNTDOWN: 'countdown',
    BOOKMARK: 'bookmark',
    AUTOPAUSE: 'autopause',
    CLOCK_PONG: 'clock.pong',
    TYPING: 'typing',
    REACTION: 'reaction',
  });

  // Structured error codes
  const ERROR_CODE = Object.freeze({
    ROOM_NOT_FOUND: 'ROOM_NOT_FOUND',
    ROOM_KEY_REQUIRED: 'ROOM_KEY_REQUIRED',
    INVALID_ROOM_KEY: 'INVALID_ROOM_KEY',
    NOT_OWNER: 'NOT_OWNER',
    COOLDOWN: 'COOLDOWN',
    VALIDATION_FAILED: 'VALIDATION_FAILED',
    PARSE_ERROR: 'PARSE_ERROR',
    USER_NOT_FOUND: 'USER_NOT_FOUND',
    RATE_LIMITED: 'RATE_LIMITED',
  });

  // Protocol version (server sends in 'ready' event)
  const PROTOCOL_VERSION = 2;

  // Default player state
  const DEFAULT_PLAYER = Object.freeze({ paused: true, buffering: true, time: 0 });

  // Validation limits (server enforces via Zod)
  const LIMITS = Object.freeze({
    MESSAGE_MAX: 300,
    USERNAME_MAX: 25,
    ROOM_NAME_MAX: 30,
    BOOKMARK_LABEL_MAX: 100,
  });

  return { C2S, S2C, ERROR_CODE, PROTOCOL_VERSION, DEFAULT_PLAYER, LIMITS };
})();
